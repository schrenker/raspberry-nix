---
machine:
  network:
    interfaces:
      - interface: end0
        dhcp: true
  install:
    disk: /dev/mmcblk0
  time:
    disabled: false # Indicates if the time service is disabled for the machine.
    servers:
      - 192.168.1.254
      - pool.ntp.org
