---
machine:
  network:
    interfaces:
      - interface: end0
        dhcp: true
  install:
    disk: /dev/mmcblk0
  time:
    disabled: false
    servers:
      - 192.168.1.254
      - pool.ntp.org
cluster:
  network:
    cni:
      name: none
  proxy:
    disabled: true
