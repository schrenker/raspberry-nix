export ISOPATH=$(expand_path ./results)
export KUBECONFIG=$(expand_path .kube/config)
export SOPS_AGE_KEY_FILE=$(expand_path ./keys.txt)
export TALOSCONFIG=$(expand_path ./talos/talosconfig)
use flake
